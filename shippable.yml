language: c

compiler:
  - gcc #8.0.1
  - clang #6.0.0

runtime:
  nodePool: Arm64
  
build: 
  pre_ci_boot:
    image_name: drydock/aarch64_u16cppall
    image_tag: master
    pull: true
    
  ci:
    - ls
